//Configuração do Ambiente: Verificação do .NET SDK:
//Para verificar se o .NET SDK está instalado, você pode usar estes comandos no terminal ou prompt de comando:

//Para verificar a versão instalada:

dotnet --version

//Para atualizar o SDK:

dotnet --list-sdks (para listar os SDKs instalados)
dotnet --list-sdks --install (para instalar a última versão disponível)

